# dependencies
node_modules

# build / cache
.next
out
build
coverage
*.tsbuildinfo

# native macOS build output (gitignored in native-macos/.gitignore, but
# Prettier only reads the ROOT .gitignore, so it must be listed here too)
native-macos/.build
native-macos/dist

# lockfiles (large, auto-generated)
package-lock.json

# generated
next-env.d.ts

# misc
.vercel
.claude/settings.local.json
.DS_Store

# workspace agent docs (leave formatting to authors)
AGENTS.md
CLAUDE.md
